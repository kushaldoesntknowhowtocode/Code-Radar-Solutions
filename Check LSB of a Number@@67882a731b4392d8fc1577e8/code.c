#include <stdio.h>

int main() {
    int num;
    // Read the input
    scanf("%d", &num);
    // Perform the bitwise one's complement operation and print the result
    printf("%d\n", ~num);
    return 0;
}
